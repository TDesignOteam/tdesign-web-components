name: PREVIEW_PR_PREVIEW

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  call-test-build:
    uses: TDesignOteam/workflows/.github/workflows/reusable-unit-test.yml@main
    with:
      node-version-file: .node-version
