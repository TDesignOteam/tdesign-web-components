# 安装时严格校验 package.json 中的 engines 字段
engine-strict=true

# pnpm link 的本地依赖不写入 lockfile，避免机器路径进仓库
excludeLinksFromLockfile=true

# peer 依赖由各 package 显式声明，避免 monorepo 安装时自动拉取尚未发布的本仓库包
auto-install-peers=false
